    SDL_Plotter g(1000,1000);
    frog_t frog;
    //title_t grass, water;
    //grass = title_t(point_t (0,0), point_t (1000,800), color_t(0, 250, 0));
    //water = title_t(point_t (0,801), point_t (1000,1000), color_t(0,10,220));
    char key, ch;
    ifstream file;
    int xLoc = 430, yLoc = 500;
    int x, y;
    int row, col;
    int size = 3;
    //bool click = false;

    /*grass.display(g);
    water.display(g);
    displayTitle(xLoc, yLoc, size, g);
    g.update();*/

    while (!g.getQuit()) {
        /*while(!click){
            g.getMouseClick(x,y);
            if(x > 425 && x < 575){
                if(y > 480 && y < 520){
                    click = true;
                }
            }
        }
        g.clear();*/
        if (g.kbhit()) {
